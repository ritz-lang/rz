[package]
name = "ritzgen"
version = "0.1.0"
description = "Ritz parser generator with combined flex/bison-style grammar DSL"

[[bin]]
name = "ritzgen"
path = "src/main.ritz"
# Note: AST is now merged into main.ritz
# Multi-file support requires proper extern declarations
