# Level 3, Test 06: Struct with multiple fields
# Test accessing multiple fields in a struct
# (Nested struct support is a known ritz1 limitation)

struct Data
    a: i32
    b: i32
    c: i32

fn main() -> i32
    var d: Data
    d.a = 10
    d.b = 12
    d.c = 20
    d.a + d.b + d.c
