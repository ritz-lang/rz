import ritzlib.memory
import ritzlib.str

fn buffer_with_data(src: *u8, len: i64) -> i64
    let data = malloc(len)
    memcpy(data, src, len)
    return data as i64

[[test]]
fn test_buffer() -> i32
    let result = buffer_with_data("hello", 5)
    0
