# Prism - Types module
#
# Re-exports all common types used throughout Prism.

pub import types.rect { Point, Size, Rect }
pub import types.color { Color }
pub import types.id { WindowId, SurfaceId, FontId, ImageId, IdGenerator }
