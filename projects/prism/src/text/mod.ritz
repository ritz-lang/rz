# Prism - Text Rendering Module
#
# Integrates with Angelo for font loading, shaping, and rasterization.
# Provides text rendering for the compositor.

pub import text.font_manager { FontManager, FontId }
pub import text.renderer { TextRenderer }
pub import text.glyph_cache { GlyphCache, CachedGlyph }
