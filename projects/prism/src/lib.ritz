# Prism - Display Server Library
#
# Core library for the Prism display server.
# This module exposes the public API for testing and integration.

pub import types
pub import protocol
pub import compositor
pub import backend
pub import input
# pub import text  # Disabled until angelo dependency is fixed

# Public API for client applications (Iris, etc.)
# This is the stable interface that clients should use.
# pub import api  # Disabled until angelo dependency is fixed
