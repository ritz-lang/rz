# Angelo - Text Shaper Module
#
# Converts text strings into positioned glyph runs.
# This is the interface between raw text and rendered glyphs.

pub import simple
pub import types

# Note: Access types via shaper.types and shaper.simple submodules
