[package]
name = "angelo"
version = "0.1.0"
description = "Font loading and rendering library for Prism"

sources = ["src"]

[[bin]]
name = "angelo-test"
entry = "main::main"

# Future: when we have dependencies
# [dependencies]
# prism = { path = "../prism" }  # For shared types (Rect, Point, etc.)
