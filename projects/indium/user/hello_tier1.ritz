# Harland Tier 1: hello
#
# Ported from: ritz/examples/tier1_basics/01_hello/src/main.ritz
# Prints "Hello, Ritz!" and exits.
# Runtime _start calls main() and then sys_exit(return_value).

import libharland { print_str }

pub fn main() -> i32
    print_str(c"Hello, Ritz!\n")
    return 0
