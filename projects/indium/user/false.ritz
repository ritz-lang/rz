# Harland Tier 1: false
#
# Ported from: ritz/examples/tier1_basics/04_true_false/src/false.ritz
# The simplest failing program - always exits with failure (1).
# Runtime _start calls main() and then sys_exit(return_value).

pub fn main() -> i32
    return 1
