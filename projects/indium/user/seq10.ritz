# seq10: Print numbers 1 to 10
#
# Simple test of number printing without needing argv.

import libharland { print_str, print_num, sys_exit }

pub fn main() -> i32
    var n: i32 = 1
    while n <= 10
        print_num(n)
        print_str(c"\n")
        n = n + 1
    sys_exit(0)
    0
