# Harland Tier 1: true
#
# Ported from: ritz/examples/tier1_basics/04_true_false/src/true.ritz
# The simplest useful program - always exits with success (0).
# Runtime _start calls main() and then sys_exit(return_value).

pub fn main() -> i32
    return 0
