[package]
name = "inventory"
version = "0.1.0"
description = "Demo inventory system for ritzunit testing"

[build]
target = "x86_64-linux"
# This is a test-only package - tests are in test/
test_only = true

[deps]
ritzlib = { path = "../ritz/ritzlib" }
