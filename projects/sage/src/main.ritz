# Sage - JavaScript Engine
# A register-based JS engine with generational GC

import ritzlib.sys
import ritzlib.io

fn main(argc: i32, argv: **u8) -> i32
    prints("Sage JavaScript Engine v0.1.0\n")
    0
