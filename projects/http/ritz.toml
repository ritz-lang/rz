[package]
name = "http"
version = "0.1.0"
description = "HTTP protocol implementation for Ritz - wire protocol, parsing, and framing for HTTP/1.x, HTTP/2, and HTTP/3"

[build]
target = "x86_64-linux"

[deps]
ritzlib = { path = "ritz/ritzlib" }
