# Python dependencies for the ritz0 bootstrap compiler.
#
# ritz0 is the Python-hosted, Ritz-compliant compiler that seeds the bootstrap
# chain (ritz0 -> ritz1 -> self-hosted ritz). It is the only stage that needs a
# Python toolchain; every later stage is compiled Ritz.

llvmlite>=0.44        # LLVM IR construction in emitter_llvmlite.py
pytest>=8.0           # ritz0 unit + integration suites

# tomllib is stdlib from 3.11; tomli is the backport for older interpreters.
tomli>=2.0; python_version < "3.11"
