import ritzlib.sys

struct Point
    x: i32
    y: i32

fn test_autoderef(p: *Point) -> i32
    p.x

fn main() -> i32
    var pt: Point = Point { x: 42, y: 20 }
    let result: i32 = test_autoderef(&pt)
    sys_exit(result)
    0
