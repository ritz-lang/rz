# Ritz Example 76: option
# Demonstrates: ritzlib.option (AGAST #1460) — Some/None over i64 and a
# struct payload, expression-arm matches, Option across a call boundary,
# option_is_some / option_is_none / option_unwrap_or and .unwrap_or().

[package]
name = "option_demo"
version = "0.1.0"

[[bin]]
name = "option_demo"
path = "src/main.ritz"
