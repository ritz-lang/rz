# Ritz Example 77: args
# Demonstrates: the idiomatic ritzlib.argspec API (AGAST #1448) — argv as
# StrViews, a struct-built spec, Option lookups and Result errors.

[package]
name = "argdemo"
version = "0.1.0"
description = "Report how a command line parses under ritzlib.argspec"

[[bin]]
name = "argdemo"
path = "src/main.ritz"
