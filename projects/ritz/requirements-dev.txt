# Python dependencies for the repo's TOOLING, as opposed to the compiler.
#
# requirements.txt is deliberately scoped to what ritz0 itself needs to run --
# its header says so, and keeping that list minimal is the point of it. Things
# that only the gates and dev tools need live here instead, and CI installs
# both.
#
# This file exists because of a mistake worth naming: tools/ci_local.py imports
# PyYAML, PyYAML was present in the developer venv, and `make tools-unit` went
# red on CI at the very first push (AGAST #1363). That is the same class as the
# runtime .o that test_ritz1_ptr_arith_chain.py asserted instead of building --
# code that works because of state the developer happens to have and a fresh
# checkout does not.
#
# tools/test_tools_requirements.py fails if a third-party module imported
# anywhere under tools/ is not declared in one of the two requirements files,
# so this list cannot silently fall behind the imports.

pyyaml>=6.0           # tools/ci_local.py parses .github/workflows/main.yml
