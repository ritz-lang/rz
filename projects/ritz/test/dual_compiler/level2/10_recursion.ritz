# Level 2, Test 10: Recursion
# Recursive factorial (small value to fit in exit code)

fn factorial(n: i32) -> i32
    if n <= 1
        return 1
    return n * factorial(n - 1)

fn main() -> i32
    # 5! = 120, but exit codes are mod 256
    # So we use 4! = 24
    factorial(4)
