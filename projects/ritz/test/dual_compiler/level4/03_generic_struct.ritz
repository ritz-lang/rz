# Level 4, Test 03: Generic struct
# Test basic generic struct instantiation

struct Pair<T>
    first: T
    second: T

fn make_pair<T>(a: T, b: T) -> Pair<T>
    Pair<T> { first: a, second: b }

fn main() -> i32
    let p: Pair<i32> = make_pair<i32>(10, 32)
    p.first + p.second
