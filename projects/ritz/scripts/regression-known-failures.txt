# Examples ritz0 cannot compile yet.  One example key per line (the path under
# examples/ with `/` flattened to `_`).  Anything NOT listed here that fails to
# compile fails the suite — this file is the boundary between "known" and
# "regression", so entries must be justified, not accumulated.
#
# Seeded 2026-09-02 from a full stage-1 run (68 passed, 17 failed).
#
# 2026-09-02: 17 -> 9.  Eight entries (33_printenv, 34_kill, 35_nohup,
# 36_timeout, 37_xargs, 38_tee, 39_time, 40_watch) were NOT compiler debt --
# they were stale pre-migration duplicates left at examples/<name>/ when the
# corpus was reorganised into tiers.  Their maintained twins live at
# examples/tier4_applications/<name>/ and all eight compile and pass.  The
# top-level copies last changed 2026-04-24 (2468f27); the tiered copies were
# migrated to StrView error helpers two days later (a87f7f2), which is
# precisely why the abandoned copies still failed.  Directories deleted.
#
# The lesson is worth keeping: an allowlist entry is a claim about the
# compiler.  Verify the example is real before accepting one.

# 2026-09-07: 9 -> 7.  74_async_tiers and 75_tier2_uring were NOT blocked on
# the async framework at all — they were the last two holdouts of the `&x` ->
# `@x` address-of migration (commit 6da1440, "tier 5 backfill — &x→@x migration
# unlocks 9 packages").  76_tier3_http, the third rung of the same
# blocking-echo -> uring-echo -> uring-HTTP ladder, was migrated at the time and
# was never on this list; these two were simply missed.  Both compile after a
# mechanical substitution of 2 and 16 address-of sites respectively.  The two
# genuine bitwise `& 0xFF` uses in 75 are untouched.
#
# The header above says an allowlist entry is a claim about the compiler.  These
# two entries made a claim about async, and the truth was a syntax migration.
# Nothing surfaced that, because the "allowlisted but COMPILES" check was a
# warn().  It is now a hard failure — see report_xpass() in regression.sh
# (AGAST #1365).
#
# Also deleted in the same change: 74_async_tiers/tier2_echo_uring.ritz, which
# was byte-identical to 75_tier2_uring/src/main.ritz and referenced by nothing.

# 2026-09-07: 7 -> 4.  Three more entries were not compiler debt either.  None
# of the three had anything to do with the async framework the grouping comment
# blamed; each was the example disagreeing with a ritzlib signature or with the
# language spec, and each is fixed in the example:
#
#   52_uring    called uring_cqe_seen(@ring) with one argument.  ritzlib has
#               declared it `(ring: *IoUring, cqe: *IoUringCqe)` for as long as
#               the signature has existed, and 19 of the 22 call sites in the
#               tree already pass both.  These three sites were the outlier and
#               `cqe` was already in scope at each.  4/4 self-tests pass.
#
#   47_lisp     two defects.  (1) `heap` is a RESERVED WORD — LANGUAGE_SPEC.md
#               §2.4 lists it, and it is the prefix allocation operator
#               (`heap expr`).  The example used it as a struct field name, so
#               this was the example violating the spec, not parser debt.
#               Field renamed to `values` (the struct's own comment calls it
#               the "value heap").  (2) prints/eprints take StrView; two sites
#               passed a `*u8`.  ritzlib provides prints_cstr/eprints_cstr for
#               exactly that.  15/15 self-tests pass.
#
#   51_loadtest one character: `string_from(c"/")`.  string_from takes StrView;
#               `c"..."` is *u8.  Fixed to `string_from("/")`.
#
# The 47_lisp and 51_loadtest failures are the "StrView != i8*" shape that
# reads like the #1290/#1288 compiler family.  They are not.  The compiler was
# right to reject all three; the diagnostic simply names LLVM types and no
# source line, which is why they were read as compiler debt for months (that
# diagnostic gap is AGAST #1317's family, still open).  Identifying the callee
# needed a temporary probe in _emit_call — guessing from the corpus picked the
# wrong site twice before the probe named `prints`.

# async — genuinely blocked on `let x: T = expr` not checking expr against T
# (AGAST #1364).  The annotation is discarded, so the error surfaces later
# against a type the programmer never wrote, which is why these three read as
# async bugs.  Fixing them needs a language decision, not just the fix.
tier5_async_53_async
tier5_async_54_async_fs
tier5_async_55_async_state_machine

# 72_raii was here, delisted 2026-09-07 (AGAST #1366).  I wrote the entry
# above earlier the same day claiming it was "verified genuine compiler debt:
# real Counter** indirection-depth mismatch".  That was wrong on both counts,
# and it was wrong because the diagnostic named nothing:
#
#     TypeError: Type of #1 arg mismatch: %Counter** != %Counter*
#
# With #1366's located diagnostic the same compile says
# `main.ritz:44:23: argument 1 of \`counter_increment\` has the wrong type`,
# and the cause is visible in one step: the example declared
# `fn counter_increment(self: @@Counter)`.  `@@T` is pointer-to-pointer.  A
# mutable reference is spelled `@&T` (LANGUAGE_SPEC.md §2.4 and the type table
# at line 297).  Two more source bugs sat behind it: no ritz.toml, so build.py
# could not build the package at all, and `print(c"...{x}...")`, which does
# not interpolate (see #1374).  Fixed, output now matches expected_output
# byte-for-byte.
#
# The general lesson, for the next person tempted to write "verified genuine
# compiler debt" in this file: an anonymous error is not evidence about whose
# bug it is.
