[package]
name = "ritzlib-tests"
version = "0.1.0"
description = "Unit tests for ritzlib standard library modules"

[build]
target = "x86_64-linux"
# This is a test-only package, no main binary
test_only = true
