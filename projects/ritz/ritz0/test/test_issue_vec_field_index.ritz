import ritzlib.gvec

struct Wrap
    values: Vec<i32>

[[test]]
fn test_vec_index_on_field_receiver() -> i32
    var v = vec_new<i32>()
    vec_push<i32>(@&v, 7)
    let w = Wrap { values: v }
    if w.values[0] == 7
        0
    else
        1
