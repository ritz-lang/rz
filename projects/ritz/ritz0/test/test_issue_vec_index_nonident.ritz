# Regression: Vec indexing should work for non-ident value expressions.
import ritzlib.gvec

fn mk() -> Vec<u8>
    var v = vec_new<u8>()
    v.push(9)
    v

[[test]]
fn test_vec_index_nonident_expr() -> i32
    if mk()[0] == 9
        0
    else
        1
