struct Point
    x: f32

impl Point
    fn scale(self: @Point, f: f32) -> f32
        self.x * f

[[test]]
fn test_method_autoborrow_on_rvalue() -> i32
    let y = Point { x: 2.0 as f32 }.scale(3.0 as f32)
    if y > (5.9 as f32) and y < (6.1 as f32)
        0
    else
        1
