# Regression: BinOp-style `as` casts should lower as casts, not identifier eval.

[[test]]
fn test_binop_as_cast_expr() -> i32
    let n: i64 = 12
    let m = n as usize
    if m == 12
        0
    else
        1
