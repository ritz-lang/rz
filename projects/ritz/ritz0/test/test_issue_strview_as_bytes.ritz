import ritzlib.strview

fn first_byte_len(s: StrView) -> i32
    let bytes = s.as_bytes()
    if bytes.len() > 0 and bytes[0] == ('A' as u8)
        1
    else
        0

[[test]]
fn test_strview_as_bytes_builtin() -> i32
    if first_byte_len("ABC") == 1
        0
    else
        1
