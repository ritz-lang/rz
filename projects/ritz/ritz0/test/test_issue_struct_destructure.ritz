# Regression: let tuple-destructuring should work on struct values.

struct Pair
    a: i32
    b: i32

fn make_pair() -> Pair
    Pair { a: 2, b: 8 }

[[test]]
fn test_struct_destructure() -> i32
    let (x, y) = make_pair()
    if x + y == 10
        0
    else
        1
