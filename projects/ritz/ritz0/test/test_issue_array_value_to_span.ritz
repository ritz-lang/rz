# Regression: array values should coerce to Span<T> for call arguments.

fn consume(data: Span<u8>, _idx: i8) -> i32
    data.len as i32

[[test]]
fn test_array_value_to_span_arg() -> i32
    let buf: [2]u8 = [1, 2]
    if consume(buf, 0 as i8) == 2
        0
    else
        1
