# test_import_lib.ritz - Library to be imported

struct Point
  x: i32
  y: i32

const ORIGIN_X: i32 = 0
const ORIGIN_Y: i32 = 0

fn add_points(a: *Point, b: *Point) -> Point
  Point { x: a.x + b.x, y: a.y + b.y }

fn point_sum(p: *Point) -> i32
  p.x + p.y
