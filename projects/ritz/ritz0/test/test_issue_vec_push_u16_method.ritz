# Regression: Vec<u16>.push should resolve to the correct generic/vector push function.
import ritzlib.gvec

[[test]]
fn test_vec_push_u16_method_resolution() -> i32
    var v = vec_new<u16>()
    v.push(3 as u16)
    if v.len() == 1 and v[0] == 3
        0
    else
        1
