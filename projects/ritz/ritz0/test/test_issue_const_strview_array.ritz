# Regression: const arrays of StrView string literals should compile and index.

const SEARCH_DIRS: [2]StrView = [
    "/usr/share/fonts",
    "/tmp"
]

[[test]]
fn test_const_strview_array_index() -> i32
    # Compile-time regression: declaration of StrView const arrays must lower.
    0
