# Regression: `pass` in expression position should compile as unit/no-op.

fn use_pass_expr(flag: bool) -> i32
    if flag
        pass
    else
        7

[[test]]
fn test_pass_expr_if_arm() -> i32
    if use_pass_expr(true) == 0 and use_pass_expr(false) == 7
        0
    else
        1
