# src/presenters/mod.ritz - Presenter layer
#
# HTTP handlers and request/response processing.
# Re-exports all presenter types.

pub import presenters.page_presenter {
    page_presenter_init,
    handle_index,
    handle_page_view,
    handle_page_edit,
    handle_page_save,
    handle_page_delete,
    handle_health
}
