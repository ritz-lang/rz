[package]
name = "nexus"
version = "0.1.0"
description = "The Ritz Knowledge Base - Documentation and wiki platform"

sources = ["src"]

[[bin]]
name = "nexus"
entry = "main::main"

[profile.debug]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
debug = false

# Core dependency - ritzlib has files in root, not src/
[dependencies]
ritzlib = { path = "../ritz/ritzlib", sources = ["."] }

# TODO: Add incrementally via TDD:
# valet = { path = "../valet" }
# zeus = { path = "../zeus" }
# mausoleum = { path = "../mausoleum" }
# tome = { path = "../tome" }
