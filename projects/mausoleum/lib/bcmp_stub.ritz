# bcmp stub - LLVM optimization may replace memcmp with bcmp
# bcmp returns 0 if equal, non-zero if different (unlike memcmp's -1/0/1)
[[extern_fn("bcmp")]]
pub fn bcmp(a: *u8, b: *u8, n: i64) -> i32
    for i in 0..n
        if *(a + i) != *(b + i)
            return 1
    return 0
