[package]
name = "lexis"
version = "0.1.0"
description = "HTML5 and CSS parser for Tempest browser"

sources = ["lib", "src"]

[[bin]]
name = "lexis"
entry = "main::main"

# Dependencies commented out until lexis core is working independently
# [dependencies]
# iris = { path = "../iris" }
# prism = { path = "../prism" }
